# Database
POSTGRES_PASSWORD=your-database-password

# JWT - Run `make generate-secret` and set in .env
JWT_SECRET=your-256-bit-secret-key-here-make-it-long-and-random

# Admin seeder (only needed on first deployment, remove after)
APP_ADMIN_USERNAME=admin
APP_ADMIN_EMAIL=admin@admin.com
APP_ADMIN_PASSWORD="admin-password"